var animIcons = lottie.loadAnimation({
  container: document.getElementById('saficons'),
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'animation/data.json' // the path to the animation json
});